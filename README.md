# CAT_DOG_CNN_Identification
CNN-based Cat vs Dog image classification using TensorFlow and Keras. The model uses convolutional and max-pooling layers to extract image features and classifies input images into two categories: Cat or Dog.
